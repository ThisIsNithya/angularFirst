import { CurrencyexhangePipe } from './currencyexhange.pipe';

describe('CurrencyexhangePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyexhangePipe();
    expect(pipe).toBeTruthy();
  });
});
